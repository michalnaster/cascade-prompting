# Copy to .env and fill in — only needed when using --provider anthropic /
# --judge-provider anthropic. The script does not load .env automatically;
# export these into your shell (e.g. `export $(cat .env | xargs)`, or use
# `python-dotenv` / `direnv` if you prefer).
ANTHROPIC_API_KEY=sk-ant-...
